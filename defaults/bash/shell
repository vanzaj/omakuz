# History control
shopt -s histappend
HISTCONTROL=ignoreboth
HISTSIZE=32768
HISTFILESIZE="${HISTSIZE}"

source /usr/share/bash-completion/bash_completion

export PATH="./bin:$HOME/.local/bin:$HOME/.local/share/omakuz/bin:$PATH"
set +h

export OMAKUZ_PATH="/home/$USER/.local/share/omakuz"
